<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group>
        <item
            android:id="@+id/profile_nav_header"
            android:title="Profile"
            android:icon="@drawable/profile"
            android:onClick="profile"
            />
        <item
            android:id="@+id/following_nav_header"
            android:title="Following"
            android:icon="@drawable/following"
            />
        <item
            android:id="@+id/credits_nav_header"
            android:title="Credits"
            android:icon="@drawable/credits"
            />
        <item
            android:id="@+id/sign_out_nav_header"
            android:title="Sign out"
            android:icon="@drawable/sign_out"
            android:onClick="signOut"
            />
    </group>
</menu>